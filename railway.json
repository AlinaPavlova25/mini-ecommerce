{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "mkdir -p instance && python init_db.py && gunicorn app:app",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
